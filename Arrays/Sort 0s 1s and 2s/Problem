# Sort an Array of 0s, 1s and 2s

## Problem Statement

Given an array containing only **0s, 1s, and 2s**, sort the array so that all **0s come first**, followed by **1s**, and then **2s**.

### Example

**Input:**
```
[0, 1, 2, 0, 1, 2]
```

**Output:**
```
[0, 0, 1, 1, 2, 2]
```
